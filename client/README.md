# HOSTLR Client

React + Vite frontend for HOSTLR, backed by Firebase Authentication and Firestore.

## Setup

1. Copy `.env.example` to `.env`.
2. Fill all `VITE_FIREBASE_*` values.
3. Install and run:

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
