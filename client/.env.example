# API base URL - REQUIRED for production build
# Development: uses Vite proxy, can leave unset
# Production: set to your API URL before building (e.g. npm run build)
# VITE_API_URL=https://api.yourdomain.com/api/v1
